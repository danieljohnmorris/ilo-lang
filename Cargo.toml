[package]
name = "ilo"
version = "0.1.0"
edition = "2024"
description = "ilo — a constructed language for AI agents"

[dependencies]
logos = "0.16.1"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
thiserror = "2.0.18"
