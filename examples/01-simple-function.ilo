-- 01: Simple function (terse syntax)

fn calculate-total
	price: number, quantity: number, tax-rate: number -> number
	? price: 10, quantity: 2, tax-rate: 0.2 == 24
	? price: 100, quantity: 1, tax-rate: 0 == 100
	let subtotal = * price quantity
	let tax = * subtotal tax-rate
	+ subtotal tax
